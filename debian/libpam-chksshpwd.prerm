#!/bin/sh

set -e

if [ "$1" = remove ] && [ "${DPKG_MAINTSCRIPT_PACKAGE_REFCOUNT:-1}" = 1 ]; then
	if [ -e /home/pi/.sshwarn ] ; then 
		rm /home/pi/.sshwarn
	fi
	pam-auth-update --package --remove chksshpwd
fi

#DEBHELPER#
